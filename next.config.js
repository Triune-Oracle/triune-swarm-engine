/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
  // keep appDir false for a simple pages-based scaffold; can be switched to true if you prefer /app/
  experimental: { appDir: false }
};